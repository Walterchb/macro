{
  "name": "treasury-macro-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "python3 scripts/validate_data.py",
    "serve": "python3 -m http.server 8080 --directory dist",
    "dev": "vite"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
